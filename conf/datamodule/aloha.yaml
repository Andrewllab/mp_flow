defaults:
  - transforms: aloha_transforms
_target_: mode.datasets.aloha_data_module.AlohaDataModule
_recursive_: false


#root_data_dir: ${root_data_dir}
repo_id: ${repo_id}
action_space: 14
proprioception_dims: 14
shuffle_val: false
num_workers: ${num_workers}
split_ratio: 0.0

#datasets:
#  repo_id: ${repo_id}
#  obs_seq_len: ${obs_seq_len}
#  act_seq_len: ${act_seq_len}
#  proprio_state: ${datamodule.proprioception_dims}
#  transforms: ${datamodule.transforms}
#  lang_dataset:
#    key: "lang"
#    batch_size: ${batch_size}
#    num_workers: ${num_workers}
#    # custom_data_path: ??

#datasets:
#  _target_: mode.datasets.aloha_data_module.AlohaDataset
#  lebot_dataset_cfg:
#    repo_id: ${repo_id}
#    obs_seq_len: ${obs_seq_len}
#    act_seq_len: ${act_seq_len}
#    fps: 50
#    observations: []
#  transforms: ${transforms}
#  key: "lang"
#  batch_size: ${batch_size}
#  num_workers: ${num_workers}


datasets:
  _target_: mode.datasets.aloha_data_module.AlohaDataset
  lebot_dataset_cfg:
    repo_id: ${repo_id}
    obs_seq_len: ${obs_seq_len}
    act_seq_len: ${act_seq_len}
    fps: 50
    observations: []
  transforms: ${datamodule.transforms}
  task_description: 'Pick up the cube with the right arm and transfer it to the left arm.'
#  key: "lang"
  batch_size: 32
  num_workers: 12