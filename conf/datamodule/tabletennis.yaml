defaults:
  - transforms: tabletennis_transforms
_target_: mode.datasets.table_tennis_datamodule_rlds.TTDataModule
_recursive_: false


root_data_dir: ${root_data_dir}
action_space: 6
proprioception_dims: 6
shuffle_val: false
num_workers: ${num_workers}
split_ratio: 0.0


#datasets:
#  _target_: mode.datasets.table_tennis_datamodule.TTDataset
#  root_data_dir: ${root_data_dir}
#  action_seq_len: ${act_seq_len}
#  transforms: ${datamodule.transforms}
#  task_description: "hit the coming ball to the opposite table"
#
#  batch_size: ${batch_size}

datasets:
  _target_: mode.datasets.table_tennis_datamodule_rlds.TTDataset
  metadata: "/home/temp_store/weiran/table_tennis/info.npz"
  action_seq_len: ${act_seq_len}
  transforms: ${datamodule.transforms}
  task_description: "hit the coming ball to the opposite table"

  batch_size: ${batch_size}